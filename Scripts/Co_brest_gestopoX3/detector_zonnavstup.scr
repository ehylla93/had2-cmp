block
{
SaveGameValue(9, 0);
SetWhenever(neni,0);
frame detect;		FRM_FindFrame (detect,"zonnavstup");
}






Whenever je(_PlayerInBox(detect)==1)
{
printf ("PLAYER zonnavstup JE");
SaveGameValue(9, 1);
SetWhenever(neni,1);

}



Whenever neni(_PlayerInBox(detect)==0)
{
printf ("PLAYER zonnavstup  NENI");
SaveGameValue(9, 0);
SetWhenever(je,1);

}