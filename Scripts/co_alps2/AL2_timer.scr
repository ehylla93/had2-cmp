
Block {
Frame obj;		FRM_FindFrame(obj, "AL2_objective");
Integer dly;
}

goto end;

OnSignal(1)
{
goto TIMER;
}

OnSignal(2)
{
EndScript();
}

Label TIMER:
Delay(60000);
dly = (_RandomInt(60000));
Delay(dly);
SendSignal(obj, 1);

Label end: