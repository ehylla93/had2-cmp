// Jeep1, guerilla driver
// -------------------------------------------------------------------------------------------------------------


block{
  
  
  //SetAlarmType(1023, true);
  //SetAlarmType(768, true);
}

// -------------------------------------------------------------------------------------------------------------

OnDeath()
{
	PlayMusic(34);
	Delay(3500);
	SetObjectiveStatus(1, 4);	// optional
	SetObjectiveStatus(2, 4);	// optional
	SetObjectiveStatus(3, 2);	// civilians
	EndScript();
}


Whenever inrange ( _PlayerInRange(100))
{
  Delay(19500);
  HUMAN_Drive("", 0);
  HUMAN_BoardVehicle("Jeep1",  false, 0);
  HUMAN_SetMODE_Crouch();
 // SetAlarmType(1023, true);
 // SetAlarmType(768, true);
  goto END;
}


// -------------------------------------------------------------------------------------------------------------

  HUMAN_SETSIGHTRANGE(10000);
  HUMAN_SETAIMODE_Defensive();
  HUMAN_SetOptimized(2);
  HUMAN_BoardVehicle("Jeep1",  true, 0);
goto END;

Label END:
