block{
	Integer aievent;
	aievent=0;

	SetAlarmType(1023,0);
	SetAlarmType(274,1);
}

HUMAN_SETSIGHTRANGE(10000);
HUMAN_SETAIMODE_Defensive();
HUMAN_Suspend(true);
HUMAN_SetOptimized(1);

OnAlarm()
{
	HUMAN_Stop();
}

OnAlarmDone()
{
	HUMAN_SetAlarm(false);
	goto END;
}

OnDeath()
{
	EndScript();
}

goto END;

Label END: