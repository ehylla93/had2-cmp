// Signal sent by PlaneGuard1-4, TreeGuard1-3 (Sygurd decides which way to take)

SaveGameValue(4, 1);	//swamp (default)

OnSignal(1)
{
EnableSignal(1, false);
SaveGameValue(4, 0);	//road
printf("sgv4 = 0, Sygurd takes road");
}