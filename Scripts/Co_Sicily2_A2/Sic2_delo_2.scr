// damaged barrel 2 
// -------------------------------------------------------------------------------------------------------------

FRAME this; 			FRM_GetMyFrame(this);

// -------------------------------------------------------------------------------------------------------------

Whenever spawn(_PlayerInRange(600))
{
	GoTo movement;
}

{
Frame FRM;		FRM_GetMyFrame(FRM);
Label movement:
Delay(20000);
FRM_CreateParticle(24, this);
Delay(55000);
FRM_CreateParticle(24, this);
Delay(30000);
GoTo movement;
}
Label end:
