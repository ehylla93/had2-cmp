// Spawnzones
// ----------------------------------------------

Block
{
Frame Obj6;		FRM_FindFrame(Obj6, "obj6");

Integer ObjCount;
		ObjCount = 0;
}

OnSignal(1)
{
ObjCount = ObjCount + 1;

If(ObjCount > 4)
{
SendSignal(Obj6, 1);
}
}
