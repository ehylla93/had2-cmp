
FRAME camera_ee;
goto END;

OnSignal(1)
{
  goto SHAKE;
}

OnSignal(2)
{
goto END;
}

Label SHAKE:
  FRM_GetActiveCamera(camera_ee);
  MakeExplosion(camera_ee, 0, 1200);
  Delay(2100);
goto SHAKE;

Label END: