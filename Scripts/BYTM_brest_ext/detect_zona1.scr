block
{
SaveGameValue(1, 0);
SetWhenever(neni,0);
Frame detect;		FRM_FindFrame (detect,"detect_zona1");
}

Whenever je(_PlayerInBox(detect))
{
printf ("PLAYER ZONA1 JE");
SaveGameValue(1, 1);
SetWhenever(neni,1);
}

Whenever neni(!_PlayerInBox(detect))
{
printf ("PLAYER ZONA1 NENI");
SaveGameValue(1, 0);
SetWhenever(je,1);
}