
Block {
Frame objective;	FRM_FindFrame(objective, "objectives");
}

SetWhenever(players, false);

OnSignal(1)	// received from objectives
{
	SetWhenever(players, true);
}

Whenever players (_MP_AllTeamMembersAreInRange(1, 10))
{
	SendSignal(objective, 5);
}