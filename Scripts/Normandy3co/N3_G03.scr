// Enemy guarding the downward bridge
// -------------------------------------------------------------------------------------------------------------

Block
{
Frame me;	FRM_GetMyFrame(me);
FRM_SwitchFaceTexture(me, "e_f040");

HUMAN_SETSIGHTRANGE(98);
HUMAN_SETHEARRANGE(98);
HUMAN_SETAIMODE_Zombie();
}

HUMAN_SetOptimized(1);
HUMAN_SetSniper(1, 98);
HUMAN_SETMODE_Crouch();
HUMAN_Suspend(1);
goto END;

// -------------------------------------------------------------------------------------------------------------

OnDeath(){
printf("N3_G03 dead");
EndScript();
}

OnAlarm(){
HUMAN_SETMODE_Crouch();
EndScript();
}

OnAlarmDone(){
HUMAN_SETMODE_Crouch();
HUMAN_SetAlarm(0);
goto END;
}

// -------------------------------------------------------------------------------------------------------------

Whenever PIR (_PlayerInRange(100))
{
printf("G03 spawns");
HUMAN_Suspend(0);
}

Whenever AlarmsOff (_LoadGameValue(99)==1)
{
DisableAlarms(1);
}

Label END: