FRAME zavora;		FRM_GetMyFrame(zavora);

OnSignal(1)
{
  SetActorState(zavora, 1);
  CheckpointJoin("j1", "j2");	
}

OnSignal(2)
{
  SetActorState(zavora, 0);
  CheckpointSeparate("j1", "j2");
}