
Block {
integer posledni;
integer predposledni;
}

Whenever X (_PlayerInRange(3) )
{
  posledni	= _LoadGameValue(73);
  SaveGameValue(72, posledni);
  SaveGameValue(73, 24);
  SaveGameValue(74, 2);
}

Delay(2000);
SetWhenever(X, True);